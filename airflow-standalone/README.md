# Python and Standalone Airflow Tutorial


```shell
pip install -r requirements.txt
```

```shell
docker exec -it webserver bash

airflow users create \
    --username airflow \
    --firstname Name \
    --lastname Surname \
    --role Admin \
    --email airflow@example.com \
    --password airflow

```


