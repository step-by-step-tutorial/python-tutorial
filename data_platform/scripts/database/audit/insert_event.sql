INSERT INTO audit.event (event_id,
                         event_version,
                         event_type,
                         event_time,
                         pipeline_name,
                         pipeline_id,
                         task_name,
                         task_id,
                         task_attempt,
                         status,
                         source_system,
                         source_uri,
                         destination_system,
                         destination_uri,
                         input_row_count,
                         output_row_count,
                         rejected_row_count,
                         duplicate_row_count,
                         schema_version,
                         checksum,
                         duration_ms,
                         error_type,
                         error_message,
                         error_stacktrace,
                         metadata,
                         streaming_topic)
VALUES (:event_id,
        :event_version,
        :event_type,
        :event_time,
        :pipeline_name,
        :pipeline_id,
        :task_name,
        :task_id,
        :task_attempt,
        :status,
        :source_system,
        :source_uri,
        :destination_system,
        :destination_uri,
        :input_row_count,
        :output_row_count,
        :rejected_row_count,
        :duplicate_row_count,
        :schema_version,
        :checksum,
        :duration_ms,
        :error_type,
        :error_message,
        :error_stacktrace,
        CAST(:metadata AS JSONB),
        :streaming_topic) ON CONFLICT DO NOTHING;
